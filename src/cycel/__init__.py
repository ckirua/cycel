"""
``cycel`` — CPython development kit.

Subpackages expose focused building blocks (for example ``cycel.crypto`` for
Ethereum-style hashing and signing, ``cycel.clock`` for high-resolution time,
and ``cycel.net`` for async HTTP). Import the subpackage you need rather than
relying on a large flat namespace at the root.
"""
